# System Execution Flow

```text
request
  -> resolve composition authority
  -> invoke providers in declared order
  -> collect published results
  -> evaluate blocking dispositions
  -> project system receipt
  -> return proven or rejected outcome
```

The harness does not parse repository pages, construct stories, invent scenes,
resolve selectors, operate capture tools, or evaluate individual observations.
