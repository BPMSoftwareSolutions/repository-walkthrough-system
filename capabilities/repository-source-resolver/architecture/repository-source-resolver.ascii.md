# Repository Source Resolver

```text
authorized request
      |
      v
observe facts
      |
      v
resolve semantic authority
      |
      v
execute resolved mechanics
      |
      v
project repository identity and revision result and receipt
```
