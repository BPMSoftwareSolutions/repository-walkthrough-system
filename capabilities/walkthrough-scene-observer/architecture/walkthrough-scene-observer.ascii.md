# Walkthrough Scene Observer

```text
authorized request
      |
      v
observe facts
      |
      v
resolve semantic authority
      |
      v
execute resolved mechanics
      |
      v
project visual observation testimony result and receipt
```
