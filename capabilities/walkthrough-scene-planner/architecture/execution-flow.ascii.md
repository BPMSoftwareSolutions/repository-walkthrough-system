# Execution Flow

```text
request
  |
  v
resolve immutable authority
  |
  v
generic semantic execution
  |-- required beats in declared sequence
  |-- admitted asset selection
  |-- scene projection
  |-- semantic transition resolution
  `-- required coverage evaluation
  |
  v
project scene plan + proof receipt
```
