# Browser Walkthrough Executor

```text
authorized request
      |
      v
observe facts
      |
      v
resolve semantic authority
      |
      v
execute resolved mechanics
      |
      v
project browser execution testimony result and receipt
```
